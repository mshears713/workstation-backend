# Secrets / local env
.env
*.env.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/

# Generated run artifacts (JSON/JSONL persistence)
data/runs/*
!data/runs/.gitkeep
data/notes/*
!data/notes/.gitkeep
data/voice_inbox/*
!data/voice_inbox/.gitkeep
data/notifications/*
!data/notifications/.gitkeep
data/entries/*
!data/entries/.gitkeep
data/issues/*
!data/issues/.gitkeep

# In-progress chunked ESP32 recordings (see app/api/streaming_capture.py) -
# purely transient scratch space, deleted on successful finalize, no
# .gitkeep needed since a fresh clone doesn't need this dir to pre-exist.
data/_streaming_tmp/

# LangGraph CLI local state
.langgraph_api/

# Editors / OS
.vscode/
.idea/
Thumbs.db
.DS_Store
